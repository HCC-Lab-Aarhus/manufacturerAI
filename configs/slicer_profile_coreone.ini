# PrusaSlicer CLI overrides — Prusa Core One+ HF0.4
# Applied on top of the built-in "0.20mm BALANCED @COREONE HF0.4" profile.
# Generated by ManufacturerAI — edit freely.

# --- Output format ---
# Force ASCII G-code (our postprocessor needs text manipulation).
# The Core One+ will show a "compatibility mode" warning — that is
# expected and safe to dismiss.
binary_gcode = 0

# --- Support ---
support_material = 1
support_material_buildplate_only = 1
support_material_spacing = 2.5
support_material_contact_distance = 0.2
support_material_xy_spacing = 50%

# --- Ironing ---
ironing = 1
ironing_type = top
ironing_flowrate = 15%
ironing_speed = 15
ironing_spacing = 0.1
